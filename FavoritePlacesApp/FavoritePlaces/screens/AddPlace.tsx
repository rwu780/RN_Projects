import {View} from 'react-native';
import PlaceForm from '../components/places/PlaceForm';

export default function AddPlace(): JSX.Element {
  return (
    <View>
      <PlaceForm />
    </View>
  );
}
